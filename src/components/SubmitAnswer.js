import React from 'react';

const SubmitAnswer = (props) => {
  return (
    <div className="submit-answer-container">
      <button className="submit-answer-button">Submit and view results</button>
      <button className="submit-answer-button"> Submit and give me another!</button>
    </div>
  )
}

export default SubmitAnswer;
